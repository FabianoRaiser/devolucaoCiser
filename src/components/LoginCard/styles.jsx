import { cor_azulClaro, } from "components/ui/variaveis";
import styled from "styled-components";

export const StyledLogin = styled.div`
    border: 2px solid ${cor_azulClaro};
    border-radius: 1rem;

    padding: 1rem;

    display: flex;
    flex-direction: column;

    gap: 0.5rem;
`